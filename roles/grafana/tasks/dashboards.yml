---
- name: "Create dashboard directory"
  ansible.builtin.file:
    path: "{{ grafana_dashboard_dir }}"
    state: directory
    owner: "{{ grafana_user }}"
    group: "{{ grafana_group }}"
    mode: "0755"

- name: "Copy dashboard files from source to target"
  with_fileglob:
    - "{{ grafana_dashboard_source_path }}/*.json"
  ansible.builtin.copy:
    src: "{{ item }}"
    dest: "{{ grafana_dashboard_dir }}/{{ item | basename }}"
    mode: "0644"
    owner: "{{ grafana_user }}"
    group: "{{ grafana_group }}"

- name: "Import Grafana dashboards to Grafana"
  with_fileglob:
    - "{{ grafana_dashboard_source_path }}/*.json"
  community.grafana.grafana_dashboard:
    grafana_url: "http://127.0.0.1:{{ grafana_port }}"
    url_username: "{{ grafana_admin_api_username }}"
    url_password: "{{ grafana_admin_api_password }}"
    state: present
    commit_message: Updated by ansible
    overwrite: false
    path: "{{ grafana_dashboard_dir }}/{{ item | basename }}"

- name: "Run | reload Grafana provisioned dashboard configurations"
  ansible.builtin.uri:
  # kics-scan ignore-line
    url: "http://127.0.0.1:{{ grafana_port }}/api/admin/provisioning/dashboards/reload"
    method: POST
    force_basic_auth: true
    user: "{{ grafana_admin_api_username }}"
    password: "{{ grafana_admin_api_password }}"
    status_code: 200
